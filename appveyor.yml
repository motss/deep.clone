platform:
  - x86
  - x64
  - ARM

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm install -g npm
  - npm install

test_script:
  - node --version
  - npm --version
  - npm test

build: off
